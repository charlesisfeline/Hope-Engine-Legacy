function trigger(params:Map<String, Dynamic>)
{
	// Self-explanatory.

	var gameZoom:Float = params.get("camGame_Zoom");
	var hudZoom:Float = params.get("camHUD_zoom");

	FlxG.camera.zoom += gameZoom;
	camHUD.zoom += hudZoom;
}